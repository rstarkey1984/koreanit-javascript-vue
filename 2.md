1. JavaScript 기본 문법

값과 표현식

변수 선언 (let, const)

기본 자료형

number

string

boolean

연산자

산술 / 비교 / 논리 연산자

2. 조건문과 코드 흐름

if / else 조건문

조건식의 참/거짓 판단

조건에 따른 코드 실행 흐름

3. 객체(Object)의 개념

객체란 무엇인가

객체를 사용하는 이유

key - value 구조 이해

4. 객체 생성과 접근

객체 리터럴 {} 문법

속성 접근 방식

점 표기법 (obj.key)

대괄호 표기법 (obj["key"])

객체 값 수정 및 추가

5. 실습

변수 선언 및 값 변경

조건문을 이용한 분기 처리

객체로 사용자 정보 표현하기

객체 데이터 출력 (console.log)

Day 2 핵심 정리

JavaScript는 값을 다루는 언어다.

변수는 값을 저장하기 위한 공간이다.

객체는 여러 값을 하나로 묶는 구조다.

객체를 통해 현실 세계의 데이터를 코드로 표현할 수 있다.

강사용 메모 (선택)

Day 2는 문법 암기보다
“데이터를 코드로 어떻게 표현하는가” 에 집중한다.