# Day 1 — 실행환경과 JavaScript 실행 흐름 이해

# 1. JavaScript 는 어디에서 실행되는가

> JavaScript는 브라우저나 Node.js 같은 JavaScript 실행 환경(JavaScript Runtime) 위에서 실행된다.

브라우저는 JavaScript 실행 환경 중 하나이며,
단순히 화면만 보여주는 프로그램이 아니라 JavaScript를 실행할 수 있는 환경이다.

브라우저는 내부적으로 여러 구성 요소로 나뉜다.

- HTML 파서 ( HTML을 해석해 화면을 만드는 역할 )

- CSS 파서 + 렌더링 엔진 ( 화면 그리기 )

- JavaScript 엔진 ( JavaScript 코드를 실행하는 역할 )

이 중에서
JavaScript 코드는
**브라우저 안에 포함된 JavaScript 엔진**에서 실행된다.

그로인해 브라우저마다 사용하는 JavaScript 엔진이 다르기 때문에,
같은 코드라도 브라우저에 따라 동작이나 결과가 달라질 수 있다.

## 브라우저별 JavaScript 엔진 정리
> 브라우저마다 내부 실행기가 다르다는 사실만 이해하고, 엔진 이름은 외울 필요 없음

| 브라우저                | JS엔진                   | 비고                |
| ------------------- | -------------------- | ----------------- |
| Internet Explorer   | **JScript / Chakra** | IE 전용, 현재 사용 중단   |
| Microsoft Edge      | **V8**               | Chromium 기반   |
| Google Chrome       | **V8**               | Chromium 기반       |
| Firefox             | **SpiderMonkey**     | Mozilla           |
| Safari              | **JavaScriptCore**   | Apple             |

> 예전에는 브라우저마다 JavaScript엔진이 달라 차이가 컸지만, 현재는 Chrome과 Edge가 같은 V8 엔진을 사용한다.

---

# 2. 개발자 도구(Console) 사용하기

브라우저에는 개발자 도구(Developer Tools) 가 있다.

자바스크립트를 학습할 때 가장 먼저, 그리고 가장 자주 보게 되는 화면이다.

## 개발자 도구 열기

- Google Chrome 기준
    - F12
    - 또는 마우스 우클릭 → 검사
    
- 열린패널 Console 탭 선택

## 가장 기본적인 출력
```javascript
console.log("안녕 JavaScript");
```

이 코드를 실행하면 Console 탭에 문자열이 그대로 출력된다.

## 개발자도구에서 Console의 역할

Console은 화면을 보는 도구가 아니라 실행 흐름을 보는 도구다.

개발 과정에서 다음 용도로 계속 사용한다.

1. 코드가 실행되었는지 확인
    - 어떤 함수가 호출됐는지
    - 어떤 이벤트가 발생했는지

2. 값 확인
    - 변수에 어떤 값이 들어있는지
    - API 응답 데이터가 어떻게 생겼는지

3. 에러 확인
    - 문법 오류
    - undefined, null 접근 오류
    - 네트워크 오류   



---

# 3. JavaScript 실행 시점

브라우저는 HTML 문서를 받아서 위에서 아래로 순서대로 해석하며 화면을 만든다.

이 과정에서 `<script>` 태그를 만나면, 브라우저는 HTML 해석을 잠시 멈추고 `<script>` 태그 안에 작성된 JavaScript 코드를 처리한다. `</script>` 태그를 만나면,
중단되었던 HTML 해석을 다시 이어서 진행한다.

`<script>` 태그는 JavaScript 문법이 아니라 HTML 문법이다.

브라우저는 `<script>` 태그를 “여기서 JavaScript를 실행하라”는 신호로 사용한다.

JavaScript는 HTML을 모두 읽은 뒤에 실행되는 것이 아니라,
`<script>` 태그 위치에 따라 해석 도중 실행된다.

## 실행 시점을 “확인 가능한” 예제

### `1-1.html` 파일내용
```html
<!DOCTYPE html> <!-- HTML 문서의 시작을 알리는 선언 -->
<html> <!-- HTML 문서 전체를 감싸는 루트 요소 -->

<body> <!-- 실제 화면에 표시될 내용이 시작되는 영역 -->

  <script>
    console.log("첫번째 <script> 시작"); // 첫번째 script 태그 실행 시점 기준점

    console.log('1. <h1 id="subject">제목</h1> 요소: ', document.getElementById("subject"));

    console.log('2. <p id="content">문단</p> 요소: ', document.getElementById("content"));
  </script>

  <h1 id="subject">제목</h1>

  <script>
    console.log("두번째 <script> 시작"); // 두번째 script 태그 실행 시점 기준점

    console.log('1. <h1 id="subject">제목</h1> 요소: ', document.getElementById("subject"));

    console.log('2. <p id="content">문단</p> 요소: ', document.getElementById("content"));
  </script>

  <p id="content">내용</p>

  <script>
    console.log("세번째 <script> 시작"); // 세번째 script 태그 실행 시점 기준점

    console.log('1. <h1 id="subject">제목</h1> 요소: ', document.getElementById("subject"));

    console.log('2. <p id="content">문단</p> 요소: ', document.getElementById("content"));
  </script>

</body>

</html>
```

### 실행 시점을 확인하기 위해 사용한 JavaScript 코드
```javascript
document.getElementById("find_me");
// 지금까지 브라우저가 읽은 HTML 중에서 id가 "find_me"인 요소를 하나 찾아 달라   
```
> `document`: 브라우저가 HTML 문서를 해석한 결과로 생성한 객체다.  
> `getElementById`: document 객체 안에 들어있는 메서드

---

# 4. 값과 변수 (Value & Variable)

> JavaScript는 **값을 만들고, 변수에 저장하고, 그 값을 사용하는 언어**다.  
> 코드를 이해하려면 먼저 **값과 변수의 차이**를 알아야 한다.


## 핵심 정리

| 구분 | 의미 | 예시 |
|---|---|---|
| 값 (Value) | JavaScript가 처리하는 실제 데이터 | `10`, `"hello"`, `true` |
| 변수 (Variable) | 값을 저장하고 다시 사용하기 위한 이름 | `count`, `message` |


## 4-1. 값(Value)이란?

값은 JavaScript가 계산하고, 비교하고, 출력할 수 있는 데이터다.

```javascript
10
"안녕 JavaScript"
true
```
위의 것들은 모두 값(Value) 이다.

## 값의 자료형 (Type)
> JavaScript에서 자료형(타입) 은 변수가 아니라 값의 종류를 의미한다.

| 타입        | 설명          | 예시              |
| --------- | ----------- | --------------- |
| number    | 숫자          | `10`, `3.14`    |
| string    | 문자열         | `"hello"`       |
| boolean   | 참 / 거짓      | `true`, `false` |
| null      | 의도적으로 비어 있음 | `null`          |
| undefined | 값이 아직 없음    | `undefined`     |
| object    | 여러 값을 묶은 형태 | `{}`, `[]`      |

## 4-2. 변수(Variable)란?
변수는 값에 이름을 붙여 저장하는 방법이다.
```javascript
let message = "안녕 JavaScript";
```
- "안녕 JavaScript" → 값
- message → 변수 이름

변수를 사용하면 같은 값을 여러 번 다시 사용할 수 있다.
```javascript
console.log(message);
console.log(message);
```

## 변수 선언 키워드 (var, let, const)
> var, let, const는 변수의 타입이 아니라 변수를 선언하는 방식(키워드) 이다.

| 키워드     | 의미            | 특징             |
| ------- | ------------- | -------------- |
| `var`   | 예전 방식 변수 선언   | 재선언 가능, 사용 비추천 |
| `let`   | 값이 바뀔 수 있는 변수 | 재할당 가능         |
| `const` | 값이 바뀌지 않는 변수  | 재할당 불가         |

## 변수 선언 예시
```javascript
// var : 예전 방식 (재선언 가능)
var a = 10;
var a = 20;      // 가능 (권장되지 않음)
a = 30;          // 재할당 가능

// let : 값이 바뀔 수 있는 변수
let b = 10;
// let b = 20;   // 재선언 불가
b = 20;          // 재할당 가능

// const : 값이 바뀌지 않는 변수
const c = 10;
// c = 20;       // 재할당 불가
```

## 4-3. JavaScript에서 변수와 값의 동작 방식

> JavaScript는 변수를 선언할 때 타입을 지정하지 않는다.   
> 실행 중에 변수가 가리키는 값에 따라 동작이 달라진다.
```javascript
let x = 10;
console.log(x + 5); // 15

x = "10";
console.log(x + 5); // "105"
```
- 변수 x는 그대로
- 변수가 가리키는 값이 바뀐다
- 값에 따라 JavaScript의 동작 방식이 달라진다

## (참고) Java vs JavaScript 변수 선언 비교
| 구분        | JavaScript            | Java          |
| --------- | --------------------- | ------------- |
| 변수 선언 키워드 | `var`, `let`, `const` | 타입 자체가 선언 키워드 |
| 타입 선언     | 없음                    | 필수            |

### JavaScript 관점에서 읽기
```javascript
let x = 10;
```
> x라는 이름의 변수를 만들고, 거기에 값 10을 넣는다.

### Java 관점에서 읽기
```java
int x = 10;
```
> 정수(int) 타입 변수 x를 선언하고, 값 10으로 초기화한다.

--- 
# 오늘의 핵심 정리 (Day 1)

- JavaScript는 브라우저나 Node.js 같은 실행 환경 위에서 실행된다

- 브라우저는 HTML을 위에서 아래로 순서대로 읽는다

- `<script>` 태그를 만나면 그 위치에서 JavaScript가 바로 실행된다

- Console은 화면이 아니라 실행 흐름을 확인하는 도구다

- JavaScript는 값을 만들고 그 값을 변수에 저장해서 필요할 때 다시 사용한다

- 변수는 let과 const로 선언한다

- JavaScript에서는 변수에 타입을 정하지 않고 실행 시점에 값의 종류에 따라 동작이 결정된다