# JavaScript 전체 강사용 가이드 (Day 1 ~ Day 5)

> 이 문서는 **강사만 참고하는 수업 운영 가이드**다.  
> 학생에게 배포하거나 화면에 공유하지 않는다.

## 목적
- 각 Day의 **교육 의도 정리**
- 수업 중 **반드시 말로 전달해야 할 핵심 멘트**
- 학생들이 헷갈리는 지점 사전 정리
- 수업 흐름이 흔들릴 때 기준점 제공

---

## 전체 과정 관통 핵심 관점

> **JavaScript는 DOM을 바꾸고,  
> 화면을 다시 그리는 것은 브라우저다**

- 결과 중심 표현: “JavaScript로 화면을 바꾼다”
- 기술적으로 정확한 표현: “JavaScript는 DOM을 바꾸고, 브라우저가 그린다”

이 **2단계 표현**을 구분해서 사용하는 것이 이 강의의 핵심이다.

---

## Day 1 — 실행환경과 JavaScript 실행 흐름

### 수업의 핵심 의도
- 문법 수업 ❌
- JavaScript가 **어디서 실행되는지** 감각 잡기
- DOM 개념의 씨앗만 심고 종료

### 반드시 전달할 핵심 멘트
- JavaScript는 혼자 실행되지 않는다
- JavaScript는 브라우저가 실행해준다
- JavaScript는 화면을 직접 그리는 언어가 아니다

### 학생에게 이렇게 말할 것
> “JavaScript는 화면을 그리는 언어가 아니라,  
> 화면을 구성하는 **값과 상태를 바꾸는 언어**다.”

---

## Day 2 — 데이터 · 함수 · 이벤트의 기본 구조

### 수업의 핵심 의도
- 문법 암기 ❌
- JavaScript 코드의 **구조 감각** 만들기

### 반드시 강조할 구조
데이터(값) → 함수(처리) → 이벤트(실행 트리거)

### 반드시 말로 전달할 문장
- “JavaScript 코드는 대부분 혼자 실행되지 않는다”
- “이벤트는 실행 버튼이다”
- “함수는 만들어졌다고 실행되지 않는다”

---

## Day 3 — DOM과 이벤트 기반 UI 제어

### 수업의 핵심 의도
- JavaScript가 **웹을 실제로 바꾸는 방식** 체험
- DOM을 “보이지 않는 객체”로 인식시키기

### 반드시 전달할 핵심 멘트
- DOM은 HTML 파일이 아니다
- DOM은 브라우저 메모리에 있는 객체다
- JavaScript는 DOM을 통해서만 화면에 접근한다

> **“JavaScript는 DOM을 바꾸고,  
> 그 DOM을 보고 화면을 다시 그리는 건 브라우저다.”**

---

## Day 4 — DOM 직접 제어의 한계와 프레임워크 등장

### 수업의 핵심 의도
- Vue 문법 수업 ❌
- “왜 프레임워크가 나왔는지” 이해시키기

### 반드시 전달할 문제의식
- 상태가 많아질수록 DOM 제어가 복잡해진다
- 데이터와 화면 코드가 섞이기 시작한다
- 코드가 커질수록 예측이 어려워진다

> “프레임워크는 DOM을 안 쓰는 게 아니다.  
> 우리가 직접 안 만지게 해주는 것이다.”

---

## Day 5 — Vue로 상태 기반 UI 흐름 체험

### 수업의 핵심 의도
- Vue를 **도구로 경험**
- 상태 → 화면 흐름 체감

### 반드시 강조할 관점
- 데이터가 중심이다
- 화면은 결과다
- DOM 직접 조작 코드가 사라진다

> “Vue는 화면을 만드는 도구가 아니라,  
> **상태와 화면을 연결해주는 도구**다.”

---

## 수업 전체를 관통하는 정리 문장

> **JavaScript는 DOM을 바꾸고,  
> 브라우저는 그 결과를 화면에 그린다**

### 최종 종료 멘트
> **프레임워크는 편법이 아니다.  
> 실수를 줄이기 위해 만들어진 도구다.**
