# JavaScript 전체 강사용 가이드 (Day 1 ~ Day 5)

이 문서는 **강사만 참고하는 수업 운영 가이드**다.  
학생에게 보여주거나 배포하지 않는다.

목적:
- 각 Day의 교육 의도 정리
- 꼭 말로 해줘야 할 설명 문장
- 학생들이 헷갈리는 지점 사전 정리
- 수업 흐름 조절 기준

---

## Day 1 — 실행환경과 JavaScript 실행 흐름

### 수업의 핵심 의도
- 문법 수업 ❌
- JavaScript를 “어디서, 누가, 어떻게 실행하는지” 감각 잡기
- DOM 개념의 씨앗만 심고 끝낸다

### 반드시 전달할 핵심 문장
- JavaScript는 혼자 실행되지 않는다
- JavaScript는 브라우저가 실행해준다
- **JavaScript는 화면을 그리는 언어가 아니라,  
  화면을 구성하는 구조와 상태를 바꾸는 언어다**

### 설명 포인트 정리
- HTML → DOM
- CSS → CSSOM
- 화면을 그리는 주체는 브라우저
- JavaScript는 DOM/CSS 값을 바꿀 뿐

### 여기서 깊게 들어가지 말 것
- 렌더 트리, 페인트, 컴포지트 ❌
- defer / async 설명 ❌ (이름만 언급 가능)

### 학생 질문 대응
Q. JavaScript로 배경색 바꾸면 그리는 거 아닌가요?  
A. 값만 바꾸고, 다시 그리는 건 브라우저다.

### 수업 마무리 멘트
- “지금 완벽히 이해 안 돼도 된다”
- “며칠 뒤 DOM 다룰 때 갑자기 연결된다”

---

## Day 2 — 데이터, 함수, 이벤트의 기본 구조

### 수업의 핵심 의도
- 문법 암기 ❌
- JavaScript 코드의 **구조 감각** 만들기
- 데이터 → 함수 → 이벤트 흐름 이해

### 반드시 강조할 흐름
- 데이터는 그냥 값이 아니다
- 함수는 실행 단위다
- 이벤트는 실행의 시작점이다

### 꼭 말해줄 문장
- “JavaScript 코드는 대부분 혼자 실행되지 않는다”
- “이벤트가 있어야 함수가 움직인다”

### 학생들이 헷갈리는 지점
- 함수 정의 vs 함수 실행
- 값과 변수의 차이
- 이벤트가 ‘자동 실행’처럼 보이는 착각

### 설명 전략
- console.log 결과 예측 → 실행
- 바로 설명하지 말고 이유 질문 던지기

---

## Day 3 — DOM 직접 제어

### 수업의 핵심 의도
- JavaScript가 실제로 웹을 어떻게 바꾸는지 체감
- DOM이 “눈에 보이지 않는 객체”라는 감각 만들기

### 핵심 설명 포인트
- document는 DOM의 시작점
- document.body는 항상 존재하는 DOM 객체
- style은 CSS 코드가 아니다 → 객체다

### 꼭 말해야 할 문장
- “DOM은 HTML 파일이 아니라,  
  브라우저 메모리 안에 만들어진 객체다”

### 여기서 주의할 점
- DOM 트리 설명 너무 깊게 ❌
- CSSOM 구조 설명 ❌
- ‘이해’보다 ‘체험’ 중심

### 다음 Day를 위한 떡밥
- DOM 직접 제어는 생각보다 귀찮다
- 코드가 조금만 커져도 관리가 어렵다

---

## Day 4 — DOM 직접 제어의 한계와 프레임워크

### 수업의 핵심 의도
- Vue 문법 수업 ❌
- “왜 이런 도구가 나왔는지” 이해시키기

### 반드시 전달할 문제의식
- 상태가 많아질수록 DOM 제어가 복잡해진다
- 화면과 데이터가 따로 논다
- 코드가 커질수록 예측이 어려워진다

### 꼭 말해줄 문장
- “프레임워크는 DOM을 안 쓰는 게 아니라,
  우리가 직접 안 만지게 해준다”

### 절대 하지 말 것
- React vs Vue 비교 ❌
- 가상 DOM 깊은 설명 ❌
- 성능 이야기 ❌

### 연결 멘트
- “이제부터는 DOM을 직접 바꾸지 않는다”
- “상태를 바꾸면 화면이 따라온다”

---

## Day 5 — Vue로 상태 기반 UI 경험

### 수업의 핵심 의도
- Vue를 ‘도구’로 경험
- 문법 암기 ❌
- 상태 → 화면 흐름 체험

### 반드시 강조할 관점
- 데이터가 중심이다
- 화면은 결과다
- DOM을 직접 만지지 않는다

### 꼭 말해줄 문장
- “Vue는 화면을 만드는 도구가 아니라,
  상태와 화면을 연결해주는 도구다”

### 학생들이 빠지는 함정
- 문법 외우려고 함
- HTML처럼 생각함

### 수업 마무리 멘트
- “지금 Vue를 다 이해할 필요 없다”
- “DOM을 직접 만지던 시절과 비교만 기억하자”

---

## 전체 과정 관통 핵심 문장

수업 전체를 관통하는 문장 하나만 기억시키면 충분하다.

> **JavaScript는 바꾸고,  
> 브라우저는 그린다**

그리고 마지막엔 이 말로 끝내면 좋다.

> **프레임워크는 DOM을 숨긴 게 아니라,  
> 우리가 실수하지 않게 도와주는 도구다**
